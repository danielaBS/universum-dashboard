import React, { Component } from 'react';
// import ErrorMessage from './Error';
// import PropTypes from 'prop-types';

class Name extends Component {
    render() {
        return (
            <div>
            
            </div>
        )
    }
}

export default Name; // Don’t forget to use export default!


Name.propTypes = {
};

Name.defaultProps = {
}